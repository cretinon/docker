ARG ARCH
ARG DISTRIB
ARG DOCKERSRC

ARG HTTP_PROXY
ARG HTTPS_PROXY

FROM ${ARCH}${DISTRIB}:stable-slim

RUN apt-get update && apt-get upgrade -y && apt-get clean && rm -rf /var/cache/apt/* /var/lib/apt/lists/*

# Clear proxy variables for final image
ENV HTTP_PROXY=
ENV HTTPS_PROXY=